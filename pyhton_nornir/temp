{% if host.facts.OSPF is defined %}
{% for policy in hosts.facts.OSPF.redistributing %}
  redistribute {{ policy }}
{% endfor %}
{% endif %}



{% for server in hosts.ciscodata.ntp.servers %}
 ntp server {{ server }}
{% endfor %}

{% for server in hosts.alldata.ntp.servers %}
 ntp server {{ server }}
{% endfor %}


{% if host.hdata.ntp.authentication.keys is defined %}
ntp authenticate
{% for key in host.hdata.ntp.authentication.keys %}
  ntp authentication-key {{loop.index}} md5 {{ key }}
{% endfor %}
{% endif %}
